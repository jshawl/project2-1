<%= form_for (@post || current_manager.posts.build) do |f| %>  <%# this or is avoiding the new, empty post being rendered in the manager's show page where all current posts are listed above the form partial %>
  <%= f.label :title %><br>
  <%= f.text_field :title %><br>

  <%= f.label :description %><br>
  <%= f.text_area :description %><br>

  <%= f.submit %>
<% end %>
