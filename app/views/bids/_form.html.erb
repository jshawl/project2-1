<%= form_for ([@user, @user.bids.build]) do |f| %>
  <p>
    <%= f.label :info %><br>
    <%= f.text_area :info %>
  </p>
  <p>
    <%= f.collection_select :post_id, Post.order(:title),:id,:title, include_blank: true %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
