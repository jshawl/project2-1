<h1><%= "#{@user.username}'s Profile" %></h1>



<h2><%= "#{@user.username}'s Applications" %></h2>
<%= render @user.bids  %>

<% if current_user %>
  <h2>Listings</h2>
  <%= render @posts %>
<% end %>
